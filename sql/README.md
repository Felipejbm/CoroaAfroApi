# Arquivos SQL

`coroa_afro_schema.sql` contém somente a estrutura legada do banco, sem dados pessoais.
Para instalações novas, prefira criar o banco vazio e iniciar a API para que o SQLAlchemy crie as tabelas atuais.
